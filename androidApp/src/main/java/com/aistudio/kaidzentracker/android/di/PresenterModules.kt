package com.aistudio.kaidzentracker.android.diimport com.aistudio.kaidzentracker.android.ui.viewmodels.*import org.koin.androidx.viewmodel.dsl.viewModelimport org.koin.dsl.moduleval presenterModules = module {    viewModel { FiveSScreenViewModel(fiveSRepository = get()) }    viewModel { KDIScreenViewModel(kdiRepository = get()) }    viewModel { StandartWorkViewModel(standartWorkRepository = get()) }    viewModel { TrmScreenViewModel(trmRepository = get()) }    viewModel { SmedViewModel(smedRepository = get()) }    viewModel { FlowScreenViewModel(flowRepository = get()) }    viewModel { StaffTrainingViewModel(staffTrainingRepository = get()) }    viewModel { LogisticScreenViewModel(logisticRepository = get()) }    viewModel { MappingScreenViewModel(mappingRepository = get()) }    viewModel { StaffInvolvementViewModel(staffInvolvementRepository = get()) }}